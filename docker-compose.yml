services:
  web:
    container_name: movie-review-movie
    build: .
    ports:
      - "31784:3000"
    environment:
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-changeme}
    volumes:
      - ./data:/app/data
